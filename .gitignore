node_modules/
dist/
coverage/
*.tsbuildinfo
.eslintcache
.env

# Personal container environment (see devcontainer.json runArgs)
.devcontainer/devcontainer.env

# Personal Claude Code settings (e.g. command permission approvals);
# shared commands in .claude/commands/ stay tracked
.claude/settings.local.json

.DS_Store
Thumbs.db

.idea/
.vscode/

# Local scratchpad: design notes, bootstrap material, and other machine-local
# files. Also holds .local/reference/, the read-only snapshot of the project
# this repo's conventions were adapted from.
.local/
