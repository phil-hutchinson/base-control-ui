// The version of doc/ruleset/rules.md that the code in src/rules/ and
// src/board/ implements. Kept as a string so "0.10" can never collapse into
// "0.1". rulesVersion.test.ts checks this against the document itself.
export const RULES_VERSION = "0.6";
