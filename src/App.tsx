import "./App.css";

/** The app shell. A placeholder welcome screen until the first story lands. */
export function App() {
  return (
    <main className="app">
      <h1 className="app__title">Base Control</h1>
      <p className="app__tagline">
        Two fleets, five contested nodes, and one hundred rounds to take the
        most ground.
      </p>
    </main>
  );
}
