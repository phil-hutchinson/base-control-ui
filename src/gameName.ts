// The game's on-screen name.

/**
 * The game's on-screen name: a working title shown in the app itself — the
 * title, the start screen and the browser tab. The repository, the package
 * and every document (`README.md`, `doc/ruleset/rules.md`) keep the name
 * Base Control until a proper branding pass says otherwise.
 */
export const GAME_NAME = "GREED";
