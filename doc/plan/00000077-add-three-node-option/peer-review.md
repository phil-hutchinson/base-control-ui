# Peer Review — 00000077 A three-node game

## Summary

The branch adds **3** as a third charged-node option, last in the group so the
start screen reads 5 4 3, with five untouched as the default and the standard
game. The rules document moves 0.30 → 0.31 in its own commit with a single
changelog entry and a matching `RULES_VERSION`; `ChargedNodeCount` widens to
`3 | 4 | 5` and `CHARGED_NODE_COUNTS` to `[5, 4, 3]`; the long-run economy
test's total-node floor becomes derived per count; Appendix B and
`nodePool.test.ts`'s tuned-constant comments gain figures measured at three
charged; the Quick Guide's stale "always four charged nodes" sentence and
`README.md` are corrected. No production file outside `nodes.ts` needed a
logic change, exactly as the plan's D8 predicted, and the new coverage runs
from the start screen through the session reducer to a three-charged board.

Verification from the repository root at HEAD: `npm run typecheck` — **pass**;
`npm run lint` — **pass** (no findings); `npm test` — **pass** (64 files, 1211
tests). `npm run format:check` reports one warning on `src/board/planetArt.ts`,
which is pre-existing (last touched by `1512e8d`, not in this branch's diff)
and is not a finding against this story.

Against the reference documents, the story's "What changes", "In scope",
"Out of scope" and "Verification" lists are all satisfied, and every plan step
is marked committed with Notes recording the inline-implementation deviations
the owner authorised. The plan does include the README step (Step 10, running
`/update-readme` plus a final prose sweep), so no comment is raised for its
absence. Nothing in the diff implements a rule the document does not state,
and the version, the constant, the changelog entry and the document all moved
together in one bump. The findings below are all Minor: three inaccurate or
speculative comment claims, one place a plan instruction was applied only
partly, and two small test-readability points.

## Comments

### Minor

| #   | Status | Resolution                                                                                                                                                                                                                        | Location                                                                                    | Comment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Suggested Change                                                                                                                                                                                                                                                                                                                           | Code Snippet                                                                                                                                                                                                                      |
| --- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Fixed  | the closing sentence now says "once they are measured at the other counts", matching line 834. No figure changed.                                                                                                                 | [doc/ruleset/rules.md#L859-L860](../../../doc/ruleset/rules.md#L859-L860)                   | Appendix B's closing paragraph still says the edge and corner figures "are expected to move only slightly once they are measured at **five**", which was true when four and five were the only counts. Two paragraphs above, the same caveat was updated to "have not been re-measured at **the other counts**" (line 834). The document now contradicts itself about which counts are outstanding — a reader of the closing line would conclude three has been measured for edges and corners, or that three does not exist. The story permitted leaving the flag "as written rather than growing a third caveat", but only one of the two instances was left as written. | Make the closing sentence match line 834: "…remain measured at four charged only, and are expected to move only slightly once they are measured at the other counts." No figures change.                                                                                                                                                   | `The edge and corner figures remain measured at four charged only,`<br>`and are expected to move only slightly once they are measured at five.`                                                                                   |
| 2   | Fixed  | the comment now says the floor drops by one per step while the ceiling and mean each drop by about two, and why — a charged node takes the depleted node it can leave behind with it.                                             | [src/rules/nodePool.test.ts#L126-L130](../../../src/rules/nodePool.test.ts#L126-L130)       | The extended comment claims the measured total-node range is "one lower throughout at each step down", but the figures it quotes drop by two at the top and in the mean: 8–13 / ≈12.9 at five, 7–11 / ≈10.9 at four, 6–9 / ≈8.9 at three. Only the minimum falls by one per step (it is the structural floor). The claim was already loose for five-versus-four before this branch, but the diff restates it as a general rule across all three counts, so it now reads as a false invariant next to the numbers that disprove it.                                                                                                                                         | Say what the figures show: the floor drops by one per step (it is `chargedNodeCount + INACTIVE_NODE_COUNT`), while the ceiling and the mean each drop by about two, because the eager driver keeps roughly as many depleted nodes alive as there are charged ones.                                                                         | `* **6 to 9, mean ≈8.9, at three** — one lower throughout at each step down,`<br>`* since one fewer charged node sits on the board at any moment.`                                                                                |
| 3   | Fixed  | the unsound cancelling claim is gone. The comment now says the pools widen as the count drops but the weighting already spreads the trio across most of the board at every count, so a wider pool buys little further separation. | [src/rules/nodePool.test.ts#L87-L91](../../../src/rules/nodePool.test.ts#L87-L91)           | The replacement explanation for why the mean refill gap does not trend with the count ("fewer charged nodes leave more room on the board, but also mean fewer refills to average across, so the two effects roughly cancel") is not sound: the number of refills a mean is pooled over affects the estimate's noise, not its expected value, so it cannot cancel a real widening of the pool. The comment states a mechanism the measurements do not support, in a file whose comments are the justification for its tuned bounds.                                                                                                                                         | Either state the observation without inventing a mechanism ("the three figures sit in a narrow band, 4.79–4.88, with no clear trend in the count"), or give a mechanism that could bias the mean (e.g. the weighting is pool-shape-driven and the extra room a smaller count leaves is mostly at distances the weighting already favours). | `* three figures sit within a narrow band rather than trending with the`<br>`* count: fewer charged nodes leave more room on the board, but also mean`<br>`* fewer refills to average across, so the two effects roughly cancel.` |
| 4   | Fixed  | every remaining screen-wide radio query in App.test.tsx is scoped to its group, via two small group helpers, with a comment saying why a value alone no longer names a radio.                                                     | [src/App.test.tsx#L41](../../../src/App.test.tsx#L41)                                       | Step 5 of the implementation plan says of `App.test.tsx` to "scope every radio query to its group (D6)", and Step 5's Notes record no deviation. The default-selection test still queries `screen.getByRole("radio", { name: "6" })` screen-wide, immediately beside a sibling assertion that is correctly scoped to the Charged nodes group; lines 48–49 and the `"45"` queries at lines 222, 282 and 305 are likewise unscoped. They pass only because no other group currently offers those names — the same latent ambiguity that made the `"3"` queries fail in Step 3.                                                                                               | Wrap the remaining queries in `within(screen.getByRole("group", { name: … }))`, matching the neighbours, or record in Step 5's Notes that only the ambiguous ones were scoped and why.                                                                                                                                                     | `expect(screen.getByRole("radio", { name: "6" })).toBeChecked();`                                                                                                                                                                 |
| 5   | Fixed  | the comment on the PLIES constants says "plies", leaving "turns" to Appendix B's player-facing quotation of the same figures.                                                                                                     | [src/rules/nodePool.test.ts#L106-L119](../../../src/rules/nodePool.test.ts#L106-L119)       | The edited comment on `MINIMUM_MEAN_PLIES_BETWEEN_REFILLS` / `MAXIMUM_MEAN_PLIES_BETWEEN_REFILLS` measures the band in "turns" ("roughly 2.2 turns at five charged, 2.8 at four and 3.7 at three") while the constants it documents are named `…PLIES…` and the same comment then says "every single ply" and "eleven-ply life". `CLAUDE.md`'s Vocabulary reserves "turn" for `rules.md`, the UI and `README.md`, and "ply" for code and tests. The wording is pre-existing, but the diff rewrote this sentence and could have brought it into line.                                                                                                                       | Say "plies" in this file and leave "turns" to Appendix B, which quotes the same figures in the player-facing word.                                                                                                                                                                                                                         | `* allowed to sit in, pooled across \`SEEDS\`. Measured at roughly _*2.2 turns`<br>`* at five charged, 2.8 at four and 3.7 at three_*`                                                                                            |
| 6   | Fixed  | the test is now an it.each table over {shipCount, chargedNodeCount, pips}, so each case names its own inputs and expectation and no assertion depends on a position.                                                              | [src/hud/ScoreDisplay.test.tsx#L129-L166](../../../src/hud/ScoreDisplay.test.tsx#L129-L166) | The pip-row test now builds six states, renders them into an array and asserts pip counts by positional index (`rendered[0]`…`rendered[5]`). The indices are already far from the states they refer to, and this story added two more; a future count would make an off-by-one silently assert the wrong case.                                                                                                                                                                                                                                                                                                                                                             | Turn the array into `it.each` over `[shipCount, chargedNodeCount, expectedPips]` tuples so each case names its own inputs and expectation, or at least assert through the named consts rather than positions.                                                                                                                              | `rendered[4].container.querySelectorAll(".score-display__pip")`                                                                                                                                                                   |
