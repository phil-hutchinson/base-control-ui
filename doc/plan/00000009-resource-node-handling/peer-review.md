# Peer Review — 00000009 Nodes: waking, charging, depleting

## Summary

This branch brings the board to life: a seeded `mulberry32` generator, a ply
counter and per-site clocks in `GameState`; §8.2 waking on touch and §8.6's
random replacement in a new `nodes.ts`; §8.7's six-step end-of-turn sequence in
a new `endOfTurn.ts`; §8.5's stranded-ship obligation in a new `stranded.ts`
with a new refusal reason; wording for every new event; and a board layer that
generalises the dampened shade to "no action available" and blinks a ship that
owes an action. The ruleset moved twice with the owner's rulings (0.3 → 0.4, a
ship on a dormant site is stranded too; 0.4 → 0.5, the freeing move is the
first action), each with a changelog entry, a `RULES_VERSION` bump and no tag,
which is correct while tagging is on hold. The clock arithmetic, the §8.3
five-collections and opponent-moves-next properties, the eighteen-ply round
trip, the draw, the safety net and the Appendix B guard are all implemented as
the document states and covered by tests.

Verification from the repository root: **`npm run typecheck` passes**,
**`npm run lint` passes**, **`npm test` passes** (28 files, 348 tests, ~31 s).
`npm run format:check` and `npm run build` were also run and pass. No rule
logic was found that contradicts `rules.md`; the findings below are one
performance/design problem, one rules-document gap the code has settled on its
own, and a set of comment-convention and wording items.

## Comments

### Major

| #   | Status   | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Location                                                                    | Comment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Suggested Change                                                                                                                                                                                                                                                                                                                               | Code Snippet                                                                                                                                                          |
| --- | -------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Resolved | Computed the owed set once per call in both `moveRefusalReason` and `legalDestinations` instead of rebuilding it twice per destination; hoisted `legalDestinations`'s ship-level checks (ownership, already-moved, §8.5) out of the per-destination filter and delegated the remaining per-square filtering to `sixOnlyLegalDestinations`, extracted into `src/rules/moveLegality.ts` alongside `sixOnlyMoveRefusalReason` (see #7). Refusal-reason ordering and every returned reason are unchanged; existing tests confirm it.                                        | [src/rules/movement.ts#L240-L275](../../../src/rules/movement.ts#L240-L275) | §8.5's obligation is a **ship-level** predicate but it is evaluated **per destination**, and twice each: `moveRefusalReason` calls `strandedObligationBinds(state)` and then `strandedShipIds(state)`, each of which rebuilds the whole owed set (and, for every candidate, runs `sixOnlyLegalDestinations`). `legalDestinations` then calls `moveRefusalReason` once per reach entry, so one call costs ~32 owed-set rebuilds. Measured on this branch: `legalDestinations` costs ~0.08 ms per ship with nothing stranded, but ~2 ms once a single ship is stranded — a ~25× cliff on the hot path `Board.tsx` renders through. | Compute the owed set once and reuse it: `const owed = strandedShipIds(state); if (owed.length > 0 && !owed.includes(shipId)) return "another-ship-stranded";`. In `legalDestinations`, hoist the ship-level checks out of the per-destination loop and delegate the rest to `sixOnlyLegalDestinations`, which already excludes §8.5 by design. | `strandedObligationBinds(state) && !strandedShipIds(state).includes(shipId)` — then `reachFrom(...).filter((d) => moveRefusalReason(state, shipId, d) === undefined)` |
| 2   | Resolved | Added a paragraph to §8.7 stating that a passed turn is still a turn and runs the end-of-turn sequence in full, so the clocks still tick and a ship of the passing player standing on a charged node still gains its shield. No code change — `ply.ts` already matched this. Recorded as a new bullet under the existing `## 0.5` entry in `changelog.md` (heading widened to cover it); `rules.md`'s version line and `RULES_VERSION` stay at 0.5 per the owner's ruling, since 0.5 is unmerged and untagged and this only states in writing what the app already did. | [src/rules/ply.ts#L64-L96](../../../src/rules/ply.ts#L64-L96)               | `applyPassGuard` runs the full §8.7 sequence for a passed ply — clocks tick and the passing side's ships on charged nodes gain shields. `rules.md` does not say this: §5 says only "their turn passes" and §8.7 says "everything that happens at the end of a turn". Plan decision 6 explicitly records the document as silent here and settles it in code. It is the natural reading, but it is gameplay-affecting (a shield and two clock ticks) and the project's rule is that code never carries a rule the document does not state.                                                                                         | Add one sentence to `rules.md` §8.7 (or §5) saying a passed turn is still a turn and runs the end-of-turn sequence, with the version bump and changelog entry that requires. No code change — the behaviour is what the story asked for.                                                                                                       | `// applyPassGuard`: `const endOfTurn = runEndOfTurn(state);` before `plyNumber: endOfTurn.state.plyNumber + 1`                                                       |

### Minor

| #   | Status   | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Location                                                                                              | Comment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Suggested Change                                                                                                                                                                                                       | Code Snippet                                                                                                                              |
| --- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------- |
| 3   | Resolved | Removed both "(plan decision 8)" citations from the doc comments, stating the behaviour directly instead.                                                                                                                                                                                                                                                                                                                                                                                                    | [src/board/announcements.ts#L45](../../../src/board/announcements.ts#L45)                             | Comment convention: two doc comments cite the implementation plan by decision number. Plan citations belong in `doc/plan/`, not in source. (Also at [#L73](../../../src/board/announcements.ts#L73).)                                                                                                                                                                                                                                                                                                                                                                              | State the behaviour without the citation: "…naming the squares once rather than repeating a sentence per ship."                                                                                                        | `* than repeating a sentence per ship (plan decision 8). A ship reaching the`                                                             |
| 4   | Resolved | Kept `SiteCooledEffect`'s first two sentences and dropped the stale "Declared here — rather than in `endOfTurn.ts`, which does not exist yet…" sentence.                                                                                                                                                                                                                                                                                                                                                     | [src/rules/nodes.ts#L105-L112](../../../src/rules/nodes.ts#L105-L112)                                 | Comment convention: `SiteCooledEffect`'s doc narrates the build order ("Declared here — rather than in `endOfTurn.ts`, which does not exist yet — so this step stands on its own"). `endOfTurn.ts` exists now, so the comment is both plan narration and stale.                                                                                                                                                                                                                                                                                                                    | Keep the first two sentences (what the effect means, who emits it) and drop the sentence about which module existed when.                                                                                              | `* in `endOfTurn.ts`, which does not exist yet — so this step stands on its`                                                              |
| 5   | Resolved | Reworded both comments to what is true under rules 0.5: the ship blinks from the start of the turn, when the obligation already binds, so the player sees which ship to move before trying another. Comment text only.                                                                                                                                                                                                                                                                                       | [src/board/BoardSquare.css#L48-L53](../../../src/board/BoardSquare.css#L48-L53)                       | Stale description of the superseded rule: the blink is explained as making the summons "visible before the obligation binds". Under rules 0.5 the obligation binds from the first action, so the blink and the binding start together — this was true only under 0.4. The same reading survives in [Board.tsx#L75-L78](../../../src/board/Board.tsx#L75-L78) ("whether or not it currently binds").                                                                                                                                                                                | Reword to what is now true: the ship blinks from the start of the turn, when the obligation already binds, so the player sees which ship to move before trying another. Simplify the `Board.tsx` comment the same way. | `visible before the obligation binds and the player has already tried the`                                                                |
| 6   | Resolved | Left `moveSentence` unchanged and instead added a regression test, `noMoveBothChargesAndEndsInABay` in `src/rules/siteSpacing.test.ts`, asserting no legal move on this board both charges a site en route and ends in a bay; added a comment in `moveSentence` naming the ordering assumption and the test that guards it.                                                                                                                                                                                  | [src/board/announcements.ts#L129-L137](../../../src/board/announcements.ts#L129-L137)                 | `moveSentence` returns the bay sentence before it looks for a `site-charged` effect, so a move that charges a site en route and ends in a bay would announce the bay and silently drop the charge. I checked every origin, shield count and reach entry on the current board: no legal move both flies over a site and ends in a bay, so this is latent, not live — but it is a silent trap.                                                                                                                                                                                       | Fold the charge clause into the bay branch too (or compute the charge clause once and append it to whichever move sentence is built), so the wording cannot depend on the board layout staying as it is.               | `if (enteredBay) { return \`… moved from ${from} into the ${to} bay and lost its shields.\`; }`                                           |
| 7   | Resolved | Extracted the §6-only pair (`sixOnlyMoveRefusalReason`, `sixOnlyLegalDestinations`) and their dependencies (`reachFrom`, `findShip`) into a new `src/rules/moveLegality.ts`; `movement.ts` and `stranded.ts` both import from it, and `stranded.ts` no longer imports from `movement.ts`, so the graph is acyclic. `movement.ts` re-exports `reachFrom`, `ReachEntry` and `MoveRefusalReason` unchanged, and `moveRefusalReason`/`legalDestinations`/`sideToMoveHasLegalMove` keep their names and meanings. | [src/rules/stranded.ts#L10](../../../src/rules/stranded.ts#L10)                                       | `movement.ts` imports `strandedShipIds`/`strandedObligationBinds` from `stranded.ts` while `stranded.ts` imports `sixOnlyLegalDestinations` from `movement.ts` — a circular module import. It works (no evaluation-time cycle) and Step 8's Notes record it deliberately, but nothing stops a future edit turning it into a real initialisation cycle, and no lint rule guards it.                                                                                                                                                                                                 | Move the §6-only pair into their own module (e.g. `src/rules/moveLegality.ts`) that both `movement.ts` and `stranded.ts` import, leaving the dependency graph acyclic.                                                 | `import { sixOnlyLegalDestinations } from "./movement";` against `import { strandedObligationBinds, strandedShipIds } from "./stranded";` |
| 8   | Resolved | Added the tagging hold to `CONTRIBUTING.md` and softened the changelog preamble to match, so both agree with `CLAUDE.md` and the entries' existing pointer to "the project's contribution notes" is now valid. The 0.4 and 0.5 entries themselves are untouched.                                                                                                                                                                                                                                             | [doc/ruleset/changelog.md#L19-L20](../../ruleset/changelog.md#L19-L20)                                | Both new entries point the reader at "the project's contribution notes" for the tagging hold, but `CONTRIBUTING.md` says the opposite — that a rules change "is tagged `rules-<version>` on `main` once merged". The hold is recorded only in `CLAUDE.md`. The changelog's own preamble (L4-L6) also still states tagging as unconditional.                                                                                                                                                                                                                                        | Either add the hold to `CONTRIBUTING.md` (and soften the changelog preamble), or change the entries' pointer to wherever the hold is actually written down.                                                            | `be a tagging candidate, but tagging is on hold until the game plays (see the project's contribution notes)`                              |
| 9   | Resolved | Reworded to "…and pays a shield to the player sitting on it at the end of each of their own turns," and rewrapped the surrounding paragraph.                                                                                                                                                                                                                                                                                                                                                                 | [README.md#L26-L28](../../../README.md#L26-L28)                                                       | Player-facing wording: "pays a shield each turn to whichever player is sitting on it" reads as one shield per turn, i.e. two a round. §8.7 step 1 grants the shield only at the end of the **holder's own** turn, so it is one a round — the cadence the plan is explicit about ("per round, not per ply").                                                                                                                                                                                                                                                                        | "…and pays a shield to the player sitting on it at the end of each of their own turns."                                                                                                                                | `> flying over it, and pays a shield each turn to whichever player is sitting`                                                            |
| 10  | Resolved | Narrowed the test to the existing three literal bay assertions, a representative sample of two more bays built via `squareLabel`, and a negative check that a non-bay square (H8) is never named "bay", keeping the completeness count assertion. No `testTimeout` added. New runtime: ~151 ms (down from ~2.6-3.0 s).                                                                                                                                                                                       | [src/board/Board.test.tsx#L63](../../../src/board/Board.test.tsx#L63)                                 | "names every bay" renders all 225 squares and now runs ~2.6-3.0 s against vitest's default 5 s `testTimeout` (up from ~2.3 s), and flaked once under load during the pipeline. The margin is thin enough that it will fail again on a loaded machine. Related: `movement.test.ts`'s 500-iteration "does not blow the stack" sweep costs ~2.4 s on its own, most of which is finding #1.                                                                                                                                                                                            | Owner's call: give this test an explicit `testTimeout` (or set one suite-wide in `vite.config.ts`), or narrow it to a sample of bays plus a negative check. Fixing #1 also cuts the `movement.test.ts` sweep sharply.  | `it("names every bay with 'bay' and no other square", () => {`                                                                            |
| 11  | Resolved | Added a one-line pointer near the top of the plan noting that Steps 13a and 13b superseded the front matter (two rules changes, shipped `RULES_VERSION` `"0.5"`), without rewriting the plan-time front matter itself.                                                                                                                                                                                                                                                                                       | [doc/plan/00000009-resource-node-handling/implementation-plan.md#L134](./implementation-plan.md#L134) | The plan's front matter was not updated after Steps 13a/13b were added mid-flight: "One rule changes (Step 1)… a version bump to **0.4**" (L26-L29), "at **version 0.3** … and **0.4** from Step 1 onwards" and "This story changes exactly one rule" (L37-L49), the code table's `RULES_VERSION → "0.4"` (L134), and Step 16's verification item 3, "passes with `RULES_VERSION` at `"0.4"`" (L1913). Two rules changes landed and the shipped value is 0.5. Step 13a records the supersession and Step 16's Notes record the stale sentence, so nothing was implemented wrongly. | For the record, correct the front matter and Step 16's item 3 to say two rules changes and `"0.5"`, or add a one-line pointer at the top of the plan to Step 13a.                                                      | `                                                                                                                                         | `src/rules/rulesVersion.ts` | `RULES_VERSION` → `"0.4"` (Step 1 only) | `   |
