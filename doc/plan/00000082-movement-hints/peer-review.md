# Peer Review — Story 00000082, The board prices the move

## Summary

The branch removes the selected ship's corner brackets and teaches the
destination and target marks to state a move's price: `legalMoves` /
`legalAttacks` hand the rules layer's `ReachEntry` (cost and all) to
`Board.tsx`, `SquareMark` becomes a cost-carrying discriminated union whose
accessible names say `costs N power`, and `BoardSquare.tsx` draws that many
copies of the hull gauge's own bar, centred, in the interaction accent. The
change is well-scoped and faithful to its documents: `rules.md`,
`changelog.md`, `RULES_VERSION`, `src/game/session.ts`,
`src/board/announcements.ts` and the Quick Guide files are all untouched (S1,
S9, S10); D3's wording is implemented verbatim; D5's geometry checks out (a
three-bar stack's outermost cap reaches ~23.9 units from centre against the
ring's 29-unit inner edge) and the stack is generated from the cost with no
ceiling (S6, pinned by a four-bar test); the accessibility ledger note from D8
is present; and no new comment cites a story number or a plan step. The two
deviations recorded in Step 6's Notes — updating `README.md` at all, and
saying "power" rather than "fuel" in it — were both the right calls: the
status paragraph already enumerates what the board shows a player, and
`README.md` says "power" throughout, so a lone "fuel" would have introduced a
second word for one substance inside one paragraph.

Verification from the repository root: `npm run typecheck` **green**,
`npm run lint` **green**, `npm test` **green** (66 files, 1293 tests, up from
the plan's 1278 baseline). `npx prettier --check .` was not required by this
review but the plan's Step 6 verification depends on it; the two pre-existing
warnings remain and are not this story's. Findings below are one Major and
six Minor; none of them is a rules discrepancy.

## Comments

### Major

| #   | Status | Resolution                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Location                                                                  | Comment                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Suggested Change                                                                                                                                                                                                                                                                                                                                         | Code Snippet                                                                                                         |
| --- | ------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| 1   | Closed | The finding's premise does not hold: a charged node whose holder walks off does not stay lit with plies remaining. `src/rules/ply.ts` sets the vacated square to `{ state: "depleted", level: EXIT_COUNTDOWN_PLIES }` the instant the holder leaves, and `countdownNumber` (`src/rules/countdown.ts`) returns `undefined` for a depleted node with no ship on it — a charged node only shows a digit once a ship has stepped onto it (`level` is 0 until then, per `NodeStatus`'s doc comment in `src/rules/gameState.ts`), and it is always occupied while it shows one. An occupied square is never a destination (`destination-occupied`, `src/rules/movement.ts`) and a ship standing on a charged node is never a target (`target-on-charged-node`, `src/rules/combat.ts`). The same facts rule out the trap-digit case too: a trapped ship occupies its square, and `target-on-depleted-node` excludes it. The bars and a countdown digit therefore cannot co-occur on any square, so no code change and no follow-up story are needed. | [src/board/BoardSquare.tsx#L253](../../../src/board/BoardSquare.tsx#L253) | A priced destination mark is drawn over a charged node's countdown digit, and neither `story.md` nor the plan considers the pair. `NodeCountdown` draws a single 44pt digit centred at (50, 50); `CostBarStack` centres its bars on the same point, so a 1-cost destination strikes one 18-long, 11-wide dark-underlaid bar straight through the digit's middle and a 3-cost destination lays three across it. This is reachable in ordinary play: a charged node whose countdown has started and whose holder then left stays lit with plies remaining (`countdownNumber` returns a number for a charged node regardless of `hasShip`), and such a square is a legal destination. The old marking overlapped too — the r-9 disc sat on the digit's centre — so this is a worsening of a pre-existing overlap rather than a new class of problem, but it is a degree the story never looked at, and Step 5's manual check list does not include landing a selection on a ticking, vacant node. | Have the owner look at a selection that reaches a ticking vacant charged node before treating this as closed. If it reads badly, the cheapest fixes are drawing the bar stack offset from the square's centre when a countdown is present, or suppressing the countdown digit on a marked square; either is a new story's work, not a silent tweak here. | `{mark?.kind === "destination" && <DestinationMark cost={mark.cost} />}` over `<text x={50} y={50} fontSize={44} …>` |

### Minor

| #   | Status   | Resolution                                                                                                                                                                                                          | Location                                                                                      | Comment                                                                                                                                                                                                                                                                                                                                                                                                                                                | Suggested Change                                                                                                                                                   | Code Snippet                                                                                                                             |
| --- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------- |
| 2   | Resolved | Re-wrapped the paragraph from the over-long line onward to the file's usual column width; no wording changed.                                                                                                       | [README.md#L96](../../../README.md#L96)                                                       | The Step 6 re-wrap left one line at 136 characters in a file otherwise wrapped at ~80. Prettier does not flag it (`proseWrap` is at its default `preserve`), so nothing in the toolchain will catch it, but it makes the paragraph's diff and its raw text read badly.                                                                                                                                                                                 | Re-wrap line 96 to the file's column width, changing no wording.                                                                                                   | `> each other — a lit node only starts running down once a ship lands on it. Each waiting node carries one, two or three rings, and the` |
| 3   | Resolved | Reworded to "…is ringed, with the same bars inside the ring when the shot costs power, and a bare ring when it is free," keeping the README's own "power" vocabulary, and re-wrapped the surrounding text to match. | [README.md#L90-L91](../../../README.md#L90-L91)                                               | "the ring carries the price of the shot the same way" reads, against the previous sentence's "a dot on every square it can reach for free, and elsewhere the … bars", as though a free shot puts a dot inside its ring. It does not — a free shot is a bare ring, which is exactly the distinction `TargetMark` preserves.                                                                                                                             | Say what a free shot draws, e.g. "…is ringed, with the same bars inside the ring when the shot costs power, and a bare ring when it is free."                      | `and the ring carries the price of the shot the same way.`                                                                               |
| 4   | Resolved | Read each map once into a `const` (`destinationCost`, `targetCost`) and test for `undefined`, dropping the second lookup and the non-null assertion.                                                                | [src/board/Board.tsx#L144-L147](../../../src/board/Board.tsx#L144-L147)                       | Each branch tests `.has(name)` and then re-reads the same key with `.get(name)!`: two lookups and a non-null assertion whose safety rests on the reader noticing the `has` two tokens earlier. It is not a correctness risk — the maps are built locally and their values are `PowerLevel` — but the assertion is avoidable, and worth avoiding here because a cost of `0` rules out the usual truthiness shorthand and so invites exactly this shape. | Read once into a `const` and test for `undefined`, which drops both the second lookup and the assertion: the compiler then carries the narrowing instead of a `!`. | `mark = { kind: "destination", cost: destinationCosts.get(name)! };`                                                                     |
| 5   | Resolved | Dropped the vacuous `--selected` assertion and trimmed the case title to "renders exactly one mark for the target square, never alongside destination".                                                             | [src/board/BoardSquare.test.tsx#L260](../../../src/board/BoardSquare.test.tsx#L260)           | D7 removed the whole selected-mark apparatus, so `.board-square__mark--selected` no longer exists anywhere in `src/`; this assertion is now vacuous and the case's title still promises "never alongside destination or selected". It is the only surviving reference to the dead class name.                                                                                                                                                          | Drop the `--selected` assertion (the preceding `toHaveLength(1)` already pins "exactly one mark") and trim the title to what it now tests.                         | `expect(container.querySelector(".board-square__mark--selected")).toBeNull();`                                                           |
| 6   | Resolved | Added assertions that each bar's `x1`/`x2` span `GAUGE_BAR_LENGTH` about x = 50 and that the enclosing group carries `stroke-linecap="round"`.                                                                      | [src/board/BoardSquare.test.tsx#L365-L384](../../../src/board/BoardSquare.test.tsx#L365-L384) | The double-stroke test pins both strokes' colour and width against the imported gauge constants, but not the two other properties that make the mark read as the gauge's bar: the round caps (`strokeLinecap="round"` on the wrapping group, which the geometry test's clearance arithmetic also assumes) and the bar's length (`GAUGE_BAR_LENGTH`). Dropping either would keep the whole suite green.                                                 | Add assertions that the bar's `x1`/`x2` span `GAUGE_BAR_LENGTH` about x 50 and that the enclosing group carries `stroke-linecap="round"`.                          | `expect(top).toHaveAttribute("stroke-width", String(GAUGE_BAR_STROKE_WIDTH));`                                                           |
| 7   | Resolved | Restored the original opening sentence and moved the `ReachEntry` / `destination` explanation into its own sentence immediately after it.                                                                           | [src/rules/combat.ts#L155-L168](../../../src/rules/combat.ts#L155-L168)                       | `legalAttacks`' doc comment nests two em-dash asides before its colon and then resumes the original sentence, so the contract ("every square within the affordable subset of its §6 movement reach holding an enemy ship") is now hard to find. The `destination`-naming aside is worth keeping; it just does not belong mid-clause.                                                                                                                   | Restore the original opening sentence and move the `ReachEntry` / `destination` explanation to a sentence of its own after it.                                     | `* Every square \`shipId\` may legally attack in the given state, each still`                                                            |
